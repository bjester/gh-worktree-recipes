# uv python project

This is a basic recipe containing `treefort` hooks that perform setup actions using `uv` for python projects. The [`uv` tool](https://docs.astral.sh/uv/getting-started/installation/) is useful for managing python projects. It can create virtual environments, install python versions, and manage dependencies.
